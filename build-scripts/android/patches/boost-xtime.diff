diff -Nur boost/thread/xtime.hpp boost/thread/xtime.hpp
--- boost/thread/xtime.hpp	2016-02-08 16:26:41.722993761 +0100
+++ boost/thread/xtime.hpp	2016-02-08 16:28:34.412417172 +0100
@@ -20,7 +20,7 @@
 
 enum xtime_clock_types
 {
-    TIME_UTC=1
+    TIME_UTC_=1
 //    TIME_TAI,
 //    TIME_MONOTONIC,
 //    TIME_PROCESS,
@@ -68,7 +68,7 @@
 
 inline int xtime_get(struct xtime* xtp, int clock_type)
 {
-    if (clock_type == TIME_UTC)
+    if (clock_type == TIME_UTC_)
     {
         *xtp=get_xtime(get_system_time());
         return clock_type;
diff -Nur libs/container/test/util.hpp libs/container/test/util.hpp
--- libs/container/test/util.hpp	2016-02-08 16:26:42.290980699 +0100
+++ libs/container/test/util.hpp	2016-02-08 22:26:02.396638793 +0100
@@ -71,7 +71,7 @@
 boost::xtime xsecs(int secs)
 {
    boost::xtime ret;
-   boost::xtime_get(&ret, boost::TIME_UTC);
+   boost::xtime_get(&ret, boost::TIME_UTC_);
    ret.sec += secs;
    return ret;
 }
diff -Nur libs/interprocess/test/condition_test_template.hpp libs/interprocess/test/condition_test_template.hpp
--- libs/interprocess/test/condition_test_template.hpp	2016-02-08 16:26:41.894989805 +0100
+++ libs/interprocess/test/condition_test_template.hpp	2016-02-08 22:25:51.532887521 +0100
@@ -47,8 +47,8 @@
     const int NANOSECONDS_PER_MILLISECOND = 1000000;
 
     boost::xtime xt;
-    int ret = boost::xtime_get(&xt, boost::TIME_UTC);
-    BOOST_INTERPROCES_CHECK(ret == static_cast<int>(boost::TIME_UTC));(void)ret;
+    int ret = boost::xtime_get(&xt, boost::TIME_UTC_);
+    BOOST_INTERPROCES_CHECK(ret == static_cast<int>(boost::TIME_UTC_));(void)ret;
     nsecs += xt.nsec;
     msecs += nsecs / NANOSECONDS_PER_MILLISECOND;
     secs += msecs / MILLISECONDS_PER_SECOND;
diff -Nur libs/interprocess/test/util.hpp libs/interprocess/test/util.hpp
--- libs/interprocess/test/util.hpp	2016-02-08 16:26:41.906989530 +0100
+++ libs/interprocess/test/util.hpp	2016-02-08 22:25:00.330051823 +0100
@@ -63,7 +63,7 @@
 boost::xtime xsecs(int secs)
 {
    boost::xtime ret;
-   boost::xtime_get(&ret, boost::TIME_UTC);
+   boost::xtime_get(&ret, boost::TIME_UTC_);
    ret.sec += secs;
    return ret;
 }
diff -Nur libs/spirit/classic/test/grammar_mt_tests.cpp libs/spirit/classic/test/grammar_mt_tests.cpp
--- libs/spirit/classic/test/grammar_mt_tests.cpp	2016-02-08 16:26:44.946919638 +0100
+++ libs/spirit/classic/test/grammar_mt_tests.cpp	2016-02-08 22:25:05.537934028 +0100
@@ -70,7 +70,7 @@
 {
     static long const nanoseconds_per_second = 1000L*1000L*1000L;
     boost::xtime xt;
-    boost::xtime_get(&xt, boost::TIME_UTC);
+    boost::xtime_get(&xt, boost::TIME_UTC_);
     xt.nsec+=1000*1000*milliseconds;
     while (xt.nsec > nanoseconds_per_second)
     {
diff -Nur libs/spirit/classic/test/owi_mt_tests.cpp libs/spirit/classic/test/owi_mt_tests.cpp
--- libs/spirit/classic/test/owi_mt_tests.cpp	2016-02-08 16:26:44.950919545 +0100
+++ libs/spirit/classic/test/owi_mt_tests.cpp	2016-02-08 22:24:18.678988488 +0100
@@ -86,7 +86,7 @@
             return test_size;
 
         boost::xtime now;
-        boost::xtime_get(&now, boost::TIME_UTC);
+        boost::xtime_get(&now, boost::TIME_UTC_);
         unsigned long seconds = now.sec - start_time.sec;
         if (seconds < 4)
         {
@@ -187,7 +187,7 @@
 void concurrent_creation_of_objects()
 {
     {
-        boost::xtime_get(&start_time, boost::TIME_UTC);
+        boost::xtime_get(&start_time, boost::TIME_UTC_);
         boost::thread thread1(callable_ref(test1));
         boost::thread thread2(callable_ref(test2));
         boost::thread thread3(callable_ref(test3));
diff -Nur libs/thread/example/starvephil.cpp libs/thread/example/starvephil.cpp
--- libs/thread/example/starvephil.cpp	2016-02-08 16:26:45.262912374 +0100
+++ libs/thread/example/starvephil.cpp	2016-02-08 22:24:30.106732483 +0100
@@ -50,7 +50,7 @@
                       << "very hot ..." << std::endl;
         }
         boost::xtime xt;
-        boost::xtime_get(&xt, boost::TIME_UTC);
+        boost::xtime_get(&xt, boost::TIME_UTC_);
         xt.sec += 3;
         boost::thread::sleep(xt);
         m_chickens += value;
@@ -85,7 +85,7 @@
             std::cout << "(" << clock() << ") Chef: cooking ..." << std::endl;
         }
         boost::xtime xt;
-        boost::xtime_get(&xt, boost::TIME_UTC);
+        boost::xtime_get(&xt, boost::TIME_UTC_);
         xt.sec += 2;
         boost::thread::sleep(xt);
         {
@@ -111,7 +111,7 @@
             if (m_id > 0)
             {
                 boost::xtime xt;
-                boost::xtime_get(&xt, boost::TIME_UTC);
+                boost::xtime_get(&xt, boost::TIME_UTC_);
                 xt.sec += 3;
                 boost::thread::sleep(xt);
             }
diff -Nur libs/thread/example/tennis.cpp libs/thread/example/tennis.cpp
--- libs/thread/example/tennis.cpp	2016-02-08 16:26:45.262912374 +0100
+++ libs/thread/example/tennis.cpp	2016-02-08 22:25:22.073559054 +0100
@@ -104,7 +104,7 @@
     boost::thread thrdb(thread_adapter(&player, (void*)PLAYER_B));
 
     boost::xtime xt;
-    boost::xtime_get(&xt, boost::TIME_UTC);
+    boost::xtime_get(&xt, boost::TIME_UTC_);
     xt.sec += 1;
     boost::thread::sleep(xt);
     {
diff -Nur libs/thread/example/thread.cpp libs/thread/example/thread.cpp
--- libs/thread/example/thread.cpp	2016-02-08 16:26:45.262912374 +0100
+++ libs/thread/example/thread.cpp	2016-02-08 22:25:09.169851792 +0100
@@ -14,7 +14,7 @@
     void operator()()
     {
         boost::xtime xt;
-        boost::xtime_get(&xt, boost::TIME_UTC);
+        boost::xtime_get(&xt, boost::TIME_UTC_);
         xt.sec += m_secs;
 
         boost::thread::sleep(xt);
diff -Nur libs/thread/example/xtime.cpp libs/thread/example/xtime.cpp
--- libs/thread/example/xtime.cpp	2016-02-08 16:26:45.262912374 +0100
+++ libs/thread/example/xtime.cpp	2016-02-08 22:24:34.530633173 +0100
@@ -10,7 +10,7 @@
 int main(int argc, char* argv[])
 {
     boost::xtime xt;
-    boost::xtime_get(&xt, boost::TIME_UTC);
+    boost::xtime_get(&xt, boost::TIME_UTC_);
     xt.sec += 1;
     boost::thread::sleep(xt); // Sleep for 1 second
 }
diff -Nur libs/thread/src/pthread/thread.cpp libs/thread/src/pthread/thread.cpp
--- libs/thread/src/pthread/thread.cpp	2016-02-08 16:26:45.262912374 +0100
+++ libs/thread/src/pthread/thread.cpp	2016-02-08 22:24:40.290503711 +0100
@@ -354,7 +354,7 @@
                     cond.timed_wait(lock, xt);
 #   endif
                     xtime cur;
-                    xtime_get(&cur, TIME_UTC);
+                    xtime_get(&cur, TIME_UTC_);
                     if (xtime_cmp(xt, cur) <= 0)
                         return;
                 }
@@ -369,7 +369,7 @@
             BOOST_VERIFY(!pthread_yield());
 #   else
             xtime xt;
-            xtime_get(&xt, TIME_UTC);
+            xtime_get(&xt, TIME_UTC_);
             sleep(xt);
 #   endif
         }
diff -Nur libs/thread/src/pthread/timeconv.inl libs/thread/src/pthread/timeconv.inl
--- libs/thread/src/pthread/timeconv.inl	2016-02-08 16:26:45.262912374 +0100
+++ libs/thread/src/pthread/timeconv.inl	2016-02-08 22:25:35.853245489 +0100
@@ -20,8 +20,8 @@
 inline void to_time(int milliseconds, boost::xtime& xt)
 {
     int res = 0;
-    res = boost::xtime_get(&xt, boost::TIME_UTC);
-    BOOST_ASSERT(res == boost::TIME_UTC); (void)res;
+    res = boost::xtime_get(&xt, boost::TIME_UTC_);
+    BOOST_ASSERT(res == boost::TIME_UTC_); (void)res;
 
     xt.sec += (milliseconds / MILLISECONDS_PER_SECOND);
     xt.nsec += ((milliseconds % MILLISECONDS_PER_SECOND) *
@@ -56,8 +56,8 @@
 {
     boost::xtime cur;
     int res = 0;
-    res = boost::xtime_get(&cur, boost::TIME_UTC);
-    BOOST_ASSERT(res == boost::TIME_UTC); (void)res;
+    res = boost::xtime_get(&cur, boost::TIME_UTC_);
+    BOOST_ASSERT(res == boost::TIME_UTC_); (void)res;
 
     if (boost::xtime_cmp(xt, cur) <= 0)
     {
@@ -87,8 +87,8 @@
 {
     boost::xtime cur;
     int res = 0;
-    res = boost::xtime_get(&cur, boost::TIME_UTC);
-    BOOST_ASSERT(res == boost::TIME_UTC); (void)res;
+    res = boost::xtime_get(&cur, boost::TIME_UTC_);
+    BOOST_ASSERT(res == boost::TIME_UTC_); (void)res;
 
     if (boost::xtime_cmp(xt, cur) <= 0)
         milliseconds = 0;
@@ -109,8 +109,8 @@
 {
     boost::xtime cur;
     int res = 0;
-    res = boost::xtime_get(&cur, boost::TIME_UTC);
-    BOOST_ASSERT(res == boost::TIME_UTC); (void)res;
+    res = boost::xtime_get(&cur, boost::TIME_UTC_);
+    BOOST_ASSERT(res == boost::TIME_UTC_); (void)res;
 
     if (boost::xtime_cmp(xt, cur) <= 0)
         microseconds = 0;
diff -Nur libs/thread/src/win32/timeconv.inl libs/thread/src/win32/timeconv.inl
--- libs/thread/src/win32/timeconv.inl	2016-02-08 16:26:45.266912281 +0100
+++ libs/thread/src/win32/timeconv.inl	2016-02-08 22:24:47.834333855 +0100
@@ -17,8 +17,8 @@
 inline void to_time(int milliseconds, boost::xtime& xt)
 {
     int res = 0;
-    res = boost::xtime_get(&xt, boost::TIME_UTC);
-    assert(res == boost::TIME_UTC);
+    res = boost::xtime_get(&xt, boost::TIME_UTC_);
+    assert(res == boost::TIME_UTC_);
 
     xt.sec += (milliseconds / MILLISECONDS_PER_SECOND);
     xt.nsec += ((milliseconds % MILLISECONDS_PER_SECOND) *
@@ -54,8 +54,8 @@
 {
     boost::xtime cur;
     int res = 0;
-    res = boost::xtime_get(&cur, boost::TIME_UTC);
-    assert(res == boost::TIME_UTC);
+    res = boost::xtime_get(&cur, boost::TIME_UTC_);
+    assert(res == boost::TIME_UTC_);
 
     if (boost::xtime_cmp(xt, cur) <= 0)
     {
@@ -85,8 +85,8 @@
 {
     boost::xtime cur;
     int res = 0;
-    res = boost::xtime_get(&cur, boost::TIME_UTC);
-    assert(res == boost::TIME_UTC);
+    res = boost::xtime_get(&cur, boost::TIME_UTC_);
+    assert(res == boost::TIME_UTC_);
 
     if (boost::xtime_cmp(xt, cur) <= 0)
         milliseconds = 0;
@@ -107,8 +107,8 @@
 {
     boost::xtime cur;
     int res = 0;
-    res = boost::xtime_get(&cur, boost::TIME_UTC);
-    assert(res == boost::TIME_UTC);
+    res = boost::xtime_get(&cur, boost::TIME_UTC_);
+    assert(res == boost::TIME_UTC_);
 
     if (boost::xtime_cmp(xt, cur) <= 0)
         microseconds = 0;
diff -Nur libs/thread/test/test_xtime.cpp libs/thread/test/test_xtime.cpp
--- libs/thread/test/test_xtime.cpp	2016-02-08 16:26:45.270912190 +0100
+++ libs/thread/test/test_xtime.cpp	2016-02-08 22:25:42.769087749 +0100
@@ -17,8 +17,8 @@
 {
     boost::xtime xt1, xt2, cur;
     BOOST_CHECK_EQUAL(
-        boost::xtime_get(&cur, boost::TIME_UTC),
-        static_cast<int>(boost::TIME_UTC));
+        boost::xtime_get(&cur, boost::TIME_UTC_),
+        static_cast<int>(boost::TIME_UTC_));
 
     xt1 = xt2 = cur;
     xt1.nsec -= 1;
@@ -42,14 +42,14 @@
     boost::xtime orig, cur, old;
     BOOST_CHECK_EQUAL(
         boost::xtime_get(&orig,
-            boost::TIME_UTC), static_cast<int>(boost::TIME_UTC));
+            boost::TIME_UTC_), static_cast<int>(boost::TIME_UTC_));
     old = orig;
 
     for (int x=0; x < 100; ++x)
     {
         BOOST_CHECK_EQUAL(
-            boost::xtime_get(&cur, boost::TIME_UTC),
-            static_cast<int>(boost::TIME_UTC));
+            boost::xtime_get(&cur, boost::TIME_UTC_),
+            static_cast<int>(boost::TIME_UTC_));
         BOOST_CHECK(boost::xtime_cmp(cur, orig) >= 0);
         BOOST_CHECK(boost::xtime_cmp(cur, old) >= 0);
         old = cur;
diff -Nur libs/thread/test/util.inl libs/thread/test/util.inl
--- libs/thread/test/util.inl	2016-02-08 16:26:45.270912190 +0100
+++ libs/thread/test/util.inl	2016-02-08 22:24:54.942173535 +0100
@@ -28,8 +28,8 @@
     const int NANOSECONDS_PER_MILLISECOND = 1000000;
 
     boost::xtime xt;
-    if (boost::TIME_UTC != boost::xtime_get (&xt, boost::TIME_UTC))
-        BOOST_ERROR ("boost::xtime_get != boost::TIME_UTC");
+    if (boost::TIME_UTC_ != boost::xtime_get (&xt, boost::TIME_UTC_))
+        BOOST_ERROR ("boost::xtime_get != boost::TIME_UTC_");
 
     nsecs += xt.nsec;
     msecs += nsecs / NANOSECONDS_PER_MILLISECOND;
